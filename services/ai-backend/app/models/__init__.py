"""Pydantic request/response models for the AI backend.

Reconstructed from usage across app.routes.* and app.services.* (the
original package was accidentally gitignored). Import directly from the
submodules, e.g. `from app.models.audio import TTSRequest`.
"""
